#include<iostream>
using namespace std;
int main(){
    int a=13, b=5;
    // cout<<(a^b);

    // bitwize and (&)
    // bitwize or (|)
    // bitwize xor (^)
    // bitwize << (left shift operator)
    // cout<<(4<<1);

    // bitwize >> (right shift operator)
    cout<<(4>>1);

}